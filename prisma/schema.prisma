datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
 
generator client {
  provider = "prisma-client-js"
}
 
model User {
  id            String          @id @default(cuid())
  name          String?
  email         String          @unique
  emailVerified DateTime?
  image         String?
  hashedPassword String?
  role          UserRole @default(USER)
  accounts      Account[]
  sessions      Session[]
  // Optional for WebAuthn support
  Authenticator Authenticator[]

  passwordResetTokens PasswordResetToken[]   // reset passw relation

 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
 
model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
 
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  @@id([provider, providerAccountId])
}
 
model Session {
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
 
model VerificationToken {
  identifier String
  token      String
  expires    DateTime
 
  @@id([identifier, token])
}
 
// Optional for WebAuthn support
model Authenticator {
  credentialID         String  @unique
  userId               String
  providerAccountId    String
  credentialPublicKey  String
  counter              Int
  credentialDeviceType String
  credentialBackedUp   Boolean
  transports           String?
 
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  @@id([userId, credentialID])
}


model PasswordResetToken {
  id        String   @id @default(cuid())
  userId    String
  tokenHash String   // simpan hash dari raw token
  expiresAt DateTime
  usedAt    DateTime?

  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model VillageProfile {
  id            String    @id @default(cuid())
  name          String                     // Nama desa
  tagline       String?                    // Nama desa
  logo          String?                     // Nama desa
  description   String?   @db.Text         // Deskripsi umum desa

  // Informasi lokasi dasar
  country       String?
  province      String?
  regency       String?                   // Kabupaten
  district      String?                   // Kecamatan
  village       String?                   // Desa / Kelurahan

  // Geografi penting
  latitude      Float?                    // Garis lintang
  longitude     Float?                    // Garis bujur
  areaKm2       Float?                    // Luas wilayah dalam km²
  elevation     Int?                      // Ketinggian dari permukaan laut (opsional)
  population    Int?                      // Jumlah penduduk

  // Visi & misi
  vision        String?   @db.Text
  mission       String?   @db.Text

  // Batas wilayah
  northBorder   String?   @db.Text
  eastBorder    String?   @db.Text
  southBorder   String?   @db.Text
  westBorder    String?   @db.Text

  // Media
  mapUrl        String?                   // URL embed Google Maps

  // Metadata
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}



model VillageConfig {
  id               Int       @id @default(autoincrement())
  villageCode      String    @unique @db.VarChar(10)
  villageName      String
  districtCode     String?   @db.VarChar(6)
  districtName     String?
  regencyCode      String?   @db.VarChar(4)
  regencyName      String?
  provinceCode     String?   @db.VarChar(2)
  provinceName     String?
  officeAddress    String?
  postalCode       String?   @db.VarChar(10)
  phone            String?
  email            String?
  website          String?
  establishedYear  Int?

  // 📝 General description
  description      String?   @db.Text

  // 🌍 Geographic info
  areaSize         Float?
  areaUnit         String?   @default("km²")
  populationTotal  Int?      @default(0)
  hamletCount      Int?      @default(0)
  rwCount          Int?      @default(0)
  rtCount          Int?      @default(0)
  borderNorth      String?
  borderEast       String?
  borderSouth      String?
  borderWest       String?
  elevation        Int?
  latitude         Decimal?  @db.Decimal(10,8)
  longitude        Decimal?  @db.Decimal(11,8)

  // 🖼️ Visual identity
  logoUrl          String?
  officePhotoUrl   String?
  vision           String?
  mission          String?
  slogan           String?

  isActive         Boolean   @default(true)
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
}



enum UserRole {
    ADMIN
    OPERATOR
    EDITOR
    USER
}